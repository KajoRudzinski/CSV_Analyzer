General file stats:
NOW:
1) Read CSV file
    DONE:   can call class that accepts a path
    DONE:   can call class that accepts a path as string
    DONE:   check if file exists
    DONE:   check if file is csv
    check if file can be opened

LATER:
2) Return number of rows and columns
3) Write number of rows and columns "log" file