General file stats:
TO DO NOW:
	Read CSV file
		All import class features tested

TO DO LATER:
	Return number of rows and columns
	Write number of rows and columns "log" file

DONE:
	Read CSV file
		can call class that accepts a path
		can call class that accepts a path as string
		check if file exists
		check if file is csv
		check if file can be read
		return DataFrame if import corect
		restrns error if impor incorrect